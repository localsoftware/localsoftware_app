export const state = () => ({
  action: [],
})

export const getters = {
  // getters
}

export const mutations = {
  // mutations
  SET_ACTION: function(state, action) {
    state.action = [action]
  },
}

export const actions = {
  // actions
}
